package Items;

import Enums.Slot;
import Enums.Type;

public class Legs extends Item{
    public Legs(Type type, String name, int levelReq, Slot slot) {
        super(type, name, levelReq, slot);
    }
}
