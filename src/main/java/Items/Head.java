package Items;

import Enums.Slot;
import Enums.Type;

public class Head extends Item {
    public Head(Type type, String name, int levelReq, Slot slot) {
        super(type, name, levelReq, slot);
    }
}
